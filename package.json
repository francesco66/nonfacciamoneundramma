{
  "name": "nonfacciamoneundramma",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "@nuxt/content": "^1.11.1",
    "core-js": "^3.8.3",
    "nuxt": "^2.14.12",
    "tachyons": "^4.12.0"
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
